extend type Mutation {
    UploadImages(id: ID!, type: String!, files: [Upload!]): [Path!]

}

type Image {
    imageable_id: ID!
    imageable_type: String!
    path: String!
}

type Path {
    path: String
}
