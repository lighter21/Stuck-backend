extend type Query {
    groups: [Group]! @all
    group(id: ID! @eq): Group @find
}

type Group {
    id: ID!
    name: String!
    privacy: String!
    description: String!
    created_at: DateTime!
    updated_at: DateTime!
    posts: [Post] @belongsToMany(relation: "Posts")
}
