type Query {
}

type Group {
    id: ID!
    name: String!
    email: String!
    created_at: DateTime!
    updated_at: DateTime!
    notifications:[Notification] @hasMany
    invitations: [Invitation] @hasMany
    posts: [Post] @hasMany
    relationships: [Relationship] @hasMany
    notifications: [Notification] @hasMany
}
